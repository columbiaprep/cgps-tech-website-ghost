{{!< default}}

<main class="site-main gh-inner">

    <div class="post-feed gh-feed">
        {{#get "posts" limit="all" filter="tags:-[course, hash-exclude]" include="tags"}}
            {{#foreach posts}}
                {{> "loop"}}
            {{/foreach}}
        {{/get}}
    </div>

</main>